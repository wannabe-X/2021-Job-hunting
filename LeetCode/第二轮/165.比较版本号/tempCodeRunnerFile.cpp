 // string t1, t2;
    // vector<string> v1, v2;

    // while (getline(s1, t1, '.'))
    // {
    //     v1.push_back(to_string(stoi(t1)));
    // }

    // while (getline(s2, t2, '.'))
    // {
    //     v2.push_back(to_string(stoi(t2)));
    // }

    // int flag = 0;
    // if (v1.size() < v2.size())
    //     flag = 1;

    // string res1 = "", res2 = "";
    // if (flag)
    // {
    //     for (int i = 0; i < v2.size(); i++)
    //     {
    //         string tmp = (i < v1.size() ? v1[i] : "0");
    //         res1 += tmp;
    //     }
    // }
    // else
    // {
    //     for (int i = 0; i < v1.size(); i++)
    //     {
    //         string tmp = (i < v2.size() ? v2[i] : "0");
    //         res2 += tmp;
    //     }
    // }

    // if (res1 > res2)
    //     return 1;
    // else if (res1 == res2)
    //     return 0;